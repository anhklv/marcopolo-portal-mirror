# リファクタリング候補レポート

対象: `app/`, `lib/`, `components/`
スキャン日: 2026-02-23
検出件数: 24件（前回19件）

---

## 前回（2026-03-09）からの解消状況

| 前回ID | 内容 | 状態 |
|--------|------|------|
| R-006 | コミュニティバッジvariant判定の共通化 | **解消** - `getCommunityBadgeVariant()` を `lib/constants/community.ts` に定義済み。`event-list.tsx` も `community.code` 経由で使用 |
| D-005 | `components/ui/tabs.tsx` 未使用 | **解消** - `event-detail.tsx` でインポート・使用中 |
| O-007 | コミュニティコードのハードコード散在 | **解消** - 全箇所で `COMMUNITY_CODE` 定数を使用 |

---

## 共通化の機会

### [R-001] Server Action内の `getAdminForPermission` 完全複製（残存・深刻度上昇）
- **優先度**: High
- **該当ファイル**:
  - `lib/actions/customer.actions.ts` (32-55行)
  - `lib/actions/event.actions.ts` (40-63行) - `// TODO: customer.actions.ts と共通化` コメントあり
  - `lib/actions/invite.actions.ts` (31-54行)
- **現状**: 3ファイルに完全に同一の関数が存在。`lib/auth/permissions.ts` の `getAuthenticatedAdmin()` と本質的に同じ処理。合計12箇所から呼ばれている
- **改善案**: `getAuthenticatedAdmin()` に統一し、3つの `getAdminForPermission()` を削除
- **影響範囲**: Server Action全般。推定削減 70行

---

### [R-002] Zodバリデーションエラーのパースロジック重複
- **優先度**: High
- **該当ファイル**:
  - `lib/actions/customer.actions.ts` (78-92行)
  - `lib/actions/event.actions.ts` (82-89行)
  - `lib/actions/event.actions.ts` (144-150行)
  - `app/admin/(authenticated)/events/_components/use-event-form.ts` (161-165行)
- **現状**: `safeParse` 失敗時に `fieldErrors: Record<string, string[]>` を構築するパターンが4箇所で同一実装
- **改善案**: `lib/validations/` に `parseZodErrors(error: ZodError): Record<string, string[]>` を定義。サーバー/クライアント両対応の純粋関数
- **影響範囲**: バリデーション処理全般。推定削減 30行

---

### [R-003] `isoToDisplay` 関数の完全複製（残存）
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/use-customer-form.ts` (80-83行)
  - `app/admin/(authenticated)/events/_components/use-event-form.ts` (60-63行)
- **現状**: ISO日付文字列を `YYYY/MM/DD` に変換する完全同一関数が2箇所に存在
- **改善案**: `lib/utils/date.ts` または `lib/utils.ts` に共通化
- **影響範囲**: フォーム初期化。推定削減 5行

---

### [R-004] 日付フォーマット関数の集約（残存）
- **優先度**: Medium
- **該当ファイル**:
  - `lib/utils.ts` (8-14行) - `formatDate()` → `YYYY/MM/DD`
  - `lib/actions/customer.actions.ts` (376-381行) - `formatDateForCsv()` → `YYYY/MM/DD`
  - `components/ui/date-picker-with-input.tsx` (23-28行) - `formatDateFromObj()` → `YYYY/MM/DD`
- **現状**: 3つの関数が全て `YYYY/MM/DD` 形式を出力する別実装。`isoToDisplay` も含めると4種類
- **改善案**: `formatDate()` に統一。`formatDateForCsv` と `formatDateFromObj` を削除
- **影響範囲**: 日付表示全般。推定削減 15行

---

### [R-005] `clearFieldError` パターンの完全複製
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/use-customer-form.ts` (198-205行)
  - `app/admin/(authenticated)/events/_components/use-event-form.ts` (127-134行)
- **現状**: `setFieldErrors` から特定フィールドを削除する完全同一関数が2箇所に存在
- **改善案**: `useFieldErrors()` カスタムフックを作成し `fieldErrors`, `setFieldErrors`, `clearFieldError` をセットで返す
- **影響範囲**: フォームフック。推定削減 10行

---

### [R-006] `NEXT_REDIRECT` エラーハンドリングの重複（残存・箇所増加）
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/use-customer-form.ts` (375-376行)
  - `app/admin/(authenticated)/events/_components/use-event-form.ts` (221-222行)
  - `app/admin/(authenticated)/customers/[id]/_components/customer-detail.tsx` (76行)
  - `app/admin/(authenticated)/events/[id]/_components/event-detail.tsx` (135行)
- **現状**: `err instanceof Error && err.message.includes("NEXT_REDIRECT")` が4箇所。Server Action追加ごとに増加する
- **改善案**: `lib/utils/error.ts` に `isRedirectError(err: unknown): boolean` を定義
- **影響範囲**: Server Action呼び出し箇所全般。推定削減 10行

---

### [R-007] `ActionResult` 型定義の不統一（残存）
- **優先度**: Medium
- **該当ファイル**:
  - `lib/actions/auth.ts` (7-9行) - `{ success: true; error: null } | { success: false; error: string }`
  - `lib/actions/customer.actions.ts` (23-26行) - `{ error?: string; fieldErrors?: ... }` （successフィールドなし）
  - `lib/actions/event.actions.ts` (31-33行) - `{ success: true; eventId: number } | { success: false; ... }`
  - `lib/actions/invite.actions.ts` (19-25行) - `{ success: true; sentCount: ... } | { success: false; ... }`
- **現状**: 4種類の `ActionResult` が異なる構造で定義。`customer.actions.ts` のみ `success` フィールドがない
- **改善案**: `lib/types/action.ts` にベース型を定義し、用途別に拡張

---

### [R-008] バッジレンダリングの `renderBadges()` 重複（残存）
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (188行)
  - `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (125行)
- **現状**: `getCustomerBadges()` を呼んで `Badge` に渡す `renderBadges()` が2箇所で類似実装
- **改善案**: `<CustomerBadges customer={customer} />` 共通コンポーネントを作成
- **影響範囲**: 顧客一覧、イベント招待。推定削減 15-20行

---

### [R-009] フィルタ表示テキスト `getFilterDisplayText()` の重複（残存）
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (138行)
  - `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (137行)
- **現状**: コミュニティフィルタの選択状態を表示するテキスト生成関数がほぼ同一実装で2箇所に存在
- **改善案**: `lib/helpers/filter-display.ts` に抽出するか、フィルタPopoverコンポーネントに内包
- **影響範囲**: フィルタUI。推定削減 15-20行

---

### [R-010] `NONE_VALUE` 定数の重複定義（残存）
- **優先度**: Low
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-form.tsx` (36行)
  - `app/admin/(authenticated)/customers/_components/audit-community-fields.tsx` (15行)
  - `app/admin/(authenticated)/customers/_components/simple-community-fields.tsx` (13行)
- **現状**: `const NONE_VALUE = "__none__"` が3ファイルで同一定義。Select未選択時のsentinel値
- **改善案**: `lib/constants/form.ts` に定義して3箇所からインポート
- **影響範囲**: 顧客フォーム関連。推定削減 5行

---

## 不要コードの削除

### [D-001] `lib/auth/permissions.ts` の `getSession()` - 不要なラッパー（残存）
- **優先度**: High
- **該当ファイル**: `lib/auth/permissions.ts` (61-63行)
- **内容**: `return auth()` を呼ぶだけの1行ラッパー関数
- **根拠**: プロダクションコード・テストのいずれからも未使用（`import.*getSession` で0件）

---

### [D-002] `components/ui/breadcrumb.tsx` - 未使用のUIコンポーネント（残存）
- **優先度**: Medium
- **該当ファイル**: `components/ui/breadcrumb.tsx` (109行)
- **根拠**: `from "@/components/ui/breadcrumb"` でgrep → プロダクションコード内0件（docsのみ）

---

### [D-003] `components/ui/collapsible.tsx` - 未使用のUIコンポーネント（残存）
- **優先度**: Medium
- **該当ファイル**: `components/ui/collapsible.tsx` (33行)
- **根拠**: `from "@/components/ui/collapsible"` でgrep → プロダクションコード内0件（docsのみ）

---

### [D-004] 未使用のバリデーション型エイリアス群（**新規**）
- **優先度**: Medium
- **該当ファイル・エクスポート名**:
  - `lib/validations/login.ts` (13行) - `LoginInput`
  - `lib/validations/customer.ts` (162行) - `CustomerInput`
  - `lib/validations/customer.ts` (163行) - `CustomerCommunityInput`
  - `lib/validations/event.ts` (53行) - `EventInput`
  - `lib/validations/event.ts` (56行) - `EventFormInput`
  - `lib/validations/admin.ts` (40行) - `AdminCreateInput`
  - `lib/validations/admin.ts` (70行) - `AdminUpdateInput`
  - `lib/validations/admin.ts` (87行) - `PasswordChangeInput`
  - `lib/validations/invite.ts` (23行) - `InviteInput`
  - `lib/validations/invite.ts` (42行) - `TestInviteInput`
- **内容**: `z.infer<typeof ...Schema>` で生成された型エイリアスが全て未使用。スキーマ自体は使われているが、型エイリアスはどこからもインポートされていない
- **根拠**: 全10個について `import.*TypeName` でgrep → 0件
- **注意**: 将来の型安全性のために意図的に残している可能性あり。削除判断は慎重に

---

### [D-005] 未使用のアクション結果型（**新規**）
- **優先度**: Medium
- **該当ファイル・エクスポート名**:
  - `lib/actions/event.actions.ts` (19行) - `DeleteEventResult`
  - `lib/actions/event.actions.ts` (23行) - `TogglePauseEventResult`
  - `lib/actions/invite.actions.ts` (19行) - `SendInviteResult`
  - `lib/actions/invite.actions.ts` (23行) - `SendTestInviteResult`
- **内容**: Server Actionの戻り値型がエクスポートされているが外部から未参照
- **根拠**: 各型名で `import.*TypeName` → 0件

---

### [D-006] エクスポート不要な型（同一ファイル内使用のみ）（**新規**）
- **優先度**: Low
- **該当ファイル・エクスポート名**:
  - `lib/repositories/customer.repository.ts` (30行) - `CustomerListFilters`
  - `lib/repositories/customer.repository.ts` (40行) - `CustomerCreateData`
  - `lib/repositories/customer.repository.ts` (61行) - `CommunityCreateData`
  - `lib/repositories/master.repository.ts` (9行) - `CustomerFormMasterData`
  - `lib/repositories/master.repository.ts` (18行) - `EventFormMasterData`
  - `lib/validations/customer.ts` (58行) - `customerSchema`（`customerFormSchema` 内で使用のみ）
  - `lib/validations/customer.ts` (144行) - `customerCommunitySchema`（同上）
  - `lib/constants/community.ts` (13行) - `CommunityCode`
  - `lib/helpers/customer-detail.ts` (11行) - `BadgeInfo`
  - `lib/helpers/customer-filter.ts` (14行) - `FilterableCustomerCommunity`
  - `lib/helpers/event-filter.ts` (25行) - `EnrichedEvent`
  - `lib/helpers/event-detail.ts` (22行) - `EventSummary`
  - `lib/types/serialized.ts` (29行) - `SerializedCommunity`
  - `lib/types/serialized.ts` (39行) - `SerializedCustomerCommunity`
- **内容**: `export` されているが同一ファイル内でしか使われていない。`export` キーワードを削除してファイルローカルにできる
- **根拠**: 各型名で外部ファイルからの `import` が0件（同一ファイル内の型参照のみ）

---

### [D-007] `event-list.tsx` の `getStatusLabel` / `getEventTypeVariant` ラッパー（残存）
- **優先度**: Low
- **該当ファイル**: `app/admin/(authenticated)/events/_components/event-list.tsx` (54-61行)
- **内容**:
  - `getEventTypeVariant(code)` → `getCommunityBadgeVariant(code, "outline")` の1行ラッパー
  - `getStatusLabel(status)` → `EVENT_STATUS_CONFIG[status].label` の1行ラッパー
- **改善案**: インラインで直接アクセスし、関数を削除
- **影響範囲**: 小。推定削減 6行

---

## その他の改善候補

### [O-001] `InviteForm` コンポーネントの巨大化（残存）
- **優先度**: Medium
- **カテゴリ**: 複雑性
- **該当ファイル**: `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (474行)
- **現状**: 3ステップ（select/customize/confirm）が全て1コンポーネント内
- **改善案**: ステップごとにサブコンポーネントに分割

---

### [O-002] `exportCustomersAction` の責務過多（残存）
- **優先度**: Medium
- **カテゴリ**: 設計
- **該当ファイル**: `lib/actions/customer.actions.ts` (259-355行, 約97行)
- **現状**: 認証・データ取得・フィルタリング・CSV生成が1つのServer Actionに集中
- **改善案**: CSV生成を `lib/helpers/csv-export.ts` に切り出し

---

### [O-003] `CustomerListFilters` の名前衝突（残存）
- **優先度**: Medium
- **カテゴリ**: 型安全性
- **該当ファイル**:
  - `lib/repositories/customer.repository.ts` (30-38行) - サーバーサイド用
  - `lib/helpers/customer-filter.ts` (4-12行) - クライアントサイド用
- **現状**: 同名の `CustomerListFilters` が2つ存在し、構造も型も異なる
- **改善案**: リポジトリ側を `RepositoryCustomerFilters` 等に改名

---

### [O-004] `form-field.tsx` の `any` 型（残存）
- **優先度**: Low
- **カテゴリ**: 型安全性
- **該当ファイル**: `components/ui/form-field.tsx` (8行)
- **現状**: `React.ReactElement<any>` で `eslint-disable` コメント付き
- **改善案**: 具体的な型に置き換え

---

### [O-005] `InviteForm` の型定義改善（残存）
- **優先度**: Low
- **カテゴリ**: 型安全性
- **該当ファイル**: `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (44-55行)
- **現状**: `InviteEvent` インターフェースが最小限の型定義。Prisma/シリアライズ型から生成すべき
- **改善案**: `lib/types/serialized.ts` に追加するか、Prismaの型を活用

---

## サマリー

| カテゴリ | High | Medium | Low | 合計 |
|---------|------|--------|-----|------|
| 共通化 | 2 | 6 | 2 | 10 |
| 不要コード | 1 | 4 | 2 | 7 |
| その他 | 0 | 3 | 2 | 5 |
| **合計** | **3** | **13** | **6** | **22** |

※ 上記の件数はD-004 (10個の未使用型)、D-005 (4個の未使用型)、D-006 (14個の不要エクスポート) をそれぞれ1件としてカウント

### 前回比

| 指標 | 前回(03/09) | 今回(02/23) | 変化 |
|------|------------|------------|------|
| 総検出件数 | 19 | 22 | +3 (新規発見による増加) |
| High | 0 | 3 | +3 (新規 + 深刻度見直し) |
| 前回からの解消 | - | 3件 | R-006, D-005, O-007 |
| 新規発見 | - | 6件 | R-002, D-004, D-005, D-006, D-007 他 |

### 推奨する対応順序

1. **[R-001] `getAdminForPermission` → `getAuthenticatedAdmin` 統一** — 3ファイル70行の完全複製。既にTODOコメントがあり対応の意思が確認できる。最大のインパクト
2. **[R-002] Zodエラーパース関数の共通化** — 4箇所の重複を1箇所に。サーバー/クライアント双方で利用可能
3. **[D-001] `getSession()` 削除** — 完全未使用の1行ラッパー。即時対応可能
4. **[D-002]-[D-003] 未使用コンポーネント削除** — `breadcrumb.tsx`, `collapsible.tsx` の2ファイル。副作用なし、合計142行削減
5. **[R-003]-[R-004] 日付フォーマット関数の統一** — `isoToDisplay`, `formatDateForCsv`, `formatDateFromObj` を `formatDate` に統一
6. **[R-005]-[R-006] フォームフック・エラーハンドリングの共通化** — `clearFieldError` フック化、`isRedirectError` ヘルパー化
7. **[R-007] `ActionResult` 型の統一** — 型設計の一貫性向上
8. **[D-004]-[D-006] 未使用エクスポートの整理** — 不要な `export` の削除。影響なしだが件数が多いので段階的に
9. **[R-008]-[R-009] バッジ・フィルタ表示の共通コンポーネント化** — UIの重複解消
10. **[O-001]-[O-002] 大規模リファクタリング** — InviteForm分割、CSVエクスポート切り出し。上記完了後に着手推奨
