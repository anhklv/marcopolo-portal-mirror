# リファクタリング候補レポート

対象: `app/`, `lib/`, `components/`
スキャン日: 2026-02-19
検出件数: 30件

---

## 共通化の機会

### [R-001] 認証・権限チェックの共通化
- **優先度**: High
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/page.tsx` (10-32行)
  - `app/admin/(authenticated)/customers/new/page.tsx` (9-31行)
  - `app/admin/(authenticated)/customers/[id]/page.tsx` (21-40行)
  - `app/admin/(authenticated)/customers/[id]/edit/page.tsx` (20-40行)
  - `app/admin/(authenticated)/events/page.tsx` (10-32行)
  - `app/admin/(authenticated)/events/[id]/invite/page.tsx` (15-54行)
- **現状**: `auth() → admin取得 → AdminForPermission構築 → scopedCommunityIds取得` のパターンが6箇所でほぼ同一のコードとして繰り返されている
- **改善案**: `lib/auth/permissions.ts` に `getAuthenticatedAdmin()` ヘルパー関数を作成し、認証チェック・admin取得・AdminForPermission構築・scopedCommunityIds取得を一括処理する。`customer.actions.ts` の `getAdminForPermission()` も統合可能
- **影響範囲**: 全ページコンポーネント。推定削減 90-120行

---

### [R-002] Dateシリアライズ処理の共通化
- **優先度**: High
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/page.tsx` (46-70行)
  - `app/admin/(authenticated)/customers/[id]/page.tsx` (52-105行)
  - `app/admin/(authenticated)/events/page.tsx` (41-59行)
  - `app/admin/(authenticated)/events/[id]/invite/page.tsx` (75-103行)
- **現状**: PrismaのDateオブジェクトを `toISOString()` に変換するシリアライズ処理が各Server Componentページで手動・個別に記述。特に `customerCommunities` のシリアライズは3箇所で繰り返されている
- **改善案**: `lib/serializers/customer.ts`, `lib/serializers/event.ts` にシリアライズ関数を集約。`serializeCustomerForList()`, `serializeCustomerForDetail()` 等
- **影響範囲**: 全Server Componentページ。推定削減 100-130行

---

### [R-003] バッジレンダリングロジックの統一
- **優先度**: High
- **該当ファイル**:
  - `lib/helpers/customer-detail.ts` (25-70行) - `getCustomerBadges()` 純粋関数
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (250-307行) - `renderBadges()`
  - `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (219-283行) - `renderBadges()`
- **現状**: 同じバッジ生成ロジックが3箇所で別々に実装されている。`customer-detail.ts` はデータのみ返す良い設計だが、他2箇所では採用されていない。ロジック細部が微妙に異なる（variantの扱い等）
- **改善案**: `getCustomerBadges()` を唯一のソースとし、各コンポーネントはその結果をBadgeに渡すだけにする。または `<CustomerBadges customer={customer} />` 共通コンポーネントを作成
- **影響範囲**: 顧客一覧、イベント招待、顧客詳細。推定削減 80-100行

---

### [R-004] マスタデータ取得の共通化
- **優先度**: High
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/new/page.tsx` (33-41行)
  - `app/admin/(authenticated)/customers/[id]/edit/page.tsx` (55-63行)
- **現状**: communities, prefectures, listingCategories, originIndustries, membershipQualifications, affiliations の `Promise.all` 取得が2箇所で完全に同一
- **改善案**: `lib/repositories/master.repository.ts` に `fetchCustomerFormMasterData()` を作成して一元化
- **影響範囲**: 顧客新規作成・編集ページ。推定削減 20-40行

---

### [R-005] ページネーションロジックの共通化
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (42, 148-168, 510-543行)
  - `app/admin/(authenticated)/events/_components/event-list.tsx` (99, 189-209, 455-490行)
- **現状**: `getPageNumbers()` 関数が完全に同一実装で2箇所に存在。`ITEMS_PER_PAGE = 10` も同値。ページネーションUIのJSXも同一構造
- **改善案**: `usePagination(items, itemsPerPage)` カスタムフックを作成。ページネーションUIも `<PaginationControls>` として切り出し
- **影響範囲**: 一覧系コンポーネント全般。推定削減 50-60行

---

### [R-006] コミュニティバッジvariant判定の共通化
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (262-272行)
  - `app/admin/(authenticated)/events/_components/event-list.tsx` (81-92行)
  - `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (227-240行)
  - `lib/helpers/customer-detail.ts` (39-42行)
- **現状**: `venture_auditor → "audit"`, `naikan_meetup → "naikan"`, `ai_club → "ai"` の判定が4箇所に散在。`event-list.tsx` は日本語コミュニティ名で分岐しており一貫性がない
- **改善案**: `lib/constants/` に `getCommunityBadgeVariant(code)` を定義し、全箇所から参照する
- **影響範囲**: 全バッジ表示箇所。推定削減 15行

---

### [R-007] 検索バー・フィルタUIの共通化
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (325-447行)
  - `app/admin/(authenticated)/events/_components/event-list.tsx` (233-334行)
  - `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (351-443行)
- **現状**: 検索バー（Search icon + Input）とコミュニティフィルタ（Popover + CheckboxItem）のUI構造が3箇所で類似
- **改善案**: `<SearchInput>`, `<FilterPopover>` 汎用コンポーネントを作成
- **影響範囲**: 一覧系コンポーネント全般。推定削減 30-40行

---

### [R-008] 配列トグルハンドラの共通化
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (171-175行)
  - `app/admin/(authenticated)/events/_components/event-list.tsx` (118-131行)
  - `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (172-195行)
- **現状**: `checked ? [...arr, value] : arr.filter(v => v !== value)` のボイラープレートが各所に散在
- **改善案**: `useArrayToggle()` カスタムフックを作成
- **影響範囲**: フィルタ機能全般。推定削減 30行

---

### [R-009] 日付フォーマット関数の集約
- **優先度**: Medium
- **該当ファイル**:
  - `lib/utils.ts` (8-14行) - `formatDate()` → `YYYY/MM/DD`
  - `lib/utils/event.ts` (6-23行) - `formatEventDate()` → `YYYY年M月D日(曜) HH:MM`
  - `lib/actions/customer.actions.ts` (377-382行) - `formatDateForCsv()` → `YYYY/MM/DD`
  - `app/admin/(authenticated)/customers/_components/customer-form.tsx` (132-138行) - `dateToIsoString()`
  - `components/ui/date-picker-with-input.tsx` (30-36行) - `formatInputDate()`
- **現状**: `formatDate()` と `formatDateForCsv()` と `formatInputDate()` が同一出力形式で別実装
- **改善案**: `lib/utils/date.ts` に全日付フォーマット関数を集約
- **影響範囲**: 日付表示全般。推定削減 20行

---

### [R-010] `checkEventStatus` と `getEventDisplayStatus` の重複統合
- **優先度**: Medium
- **該当ファイル**:
  - `lib/auth/permissions.ts` (157-177行) - `checkEventStatus()`
  - `lib/utils/event.ts` (32-56行) - `getEventDisplayStatus()`
- **現状**: ほぼ同じロジックだが引数の型が微妙に異なる。`checkEventStatus` は `permissions.ts` に配置されているが権限チェックとは無関係
- **改善案**: `getEventDisplayStatus()` に統一し、`checkEventStatus` を削除してインポートに置き換える
- **影響範囲**: イベントステータス表示。推定削減 20行

---

### [R-011] MEMBER_CATEGORY_LABELS の散在定義
- **優先度**: Medium
- **該当ファイル**:
  - `lib/constants/customer.ts` (10-14行) - 正式定義 `Record<MemberCategory, string>`
  - `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (51-55行) - ローカル重複定義 `Record<string, string>`
  - `lib/actions/customer.actions.ts` (323行) - インラインで再定義
- **現状**: 正規の定数が存在するのに、別ファイルでローカル再定義。型も異なる
- **改善案**: `lib/constants/customer.ts` からインポートに統一
- **影響範囲**: 小。推定削減 10行

---

### [R-012] Server Action内の認証・バリデーション前処理の共通化
- **優先度**: Medium
- **該当ファイル**:
  - `lib/actions/customer.actions.ts` - `createCustomerAction` (129-173行), `updateCustomerAction` (178-229行)
- **現状**: 認証 → バリデーション → スコープ検証 → メール重複チェックの流れが約70%同一構造
- **改善案**: `prepareCustomerAction(formData)` ヘルパーとして共通前処理を切り出す
- **影響範囲**: 顧客操作のServer Action。推定削減 30行

---

### [R-013] 型定義の散在
- **優先度**: Low
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (48-108行)
  - `app/admin/(authenticated)/customers/[id]/_components/customer-detail.tsx` (44-101行)
  - `app/admin/(authenticated)/customers/_components/customer-form.tsx` (42-103行)
  - `app/admin/(authenticated)/events/_components/event-list.tsx` (50-78行)
- **現状**: `CommunityOption`, `MasterData`, `SerializedCustomer` 等がそれぞれのコンポーネント内でローカル定義。`CommunityOption` は2箇所で同一定義
- **改善案**: `lib/types/serialized.ts` に共通シリアライズ型を集約
- **影響範囲**: R-002のシリアライザ共通化と同時に対応。推定削減 50-60行

---

### [R-014] コミュニティ詳細セクションのUI重複
- **優先度**: Low
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-form.tsx` - ないかんMeetup (526-560行), AI部会 (563-597行)
  - `app/admin/(authenticated)/customers/[id]/_components/customer-detail.tsx` - ないかんMeetup (227-249行), AI部会 (252-274行)
- **現状**: ないかんMeetupとAI部会のフォーム/詳細表示がほぼ同一構造
- **改善案**: `<CommunityMembershipSection>`, `<CommunityDetailSection>` 共通コンポーネントを作成
- **影響範囲**: 顧客フォーム、顧客詳細。推定削減 50-60行

---

## 不要コードの削除

### [D-001] `lib/types/index.ts` - ファイルごと未使用
- **優先度**: High
- **該当ファイル**: `lib/types/index.ts`
- **内容**: Prisma型の再エクスポート（全24型）、`EventDisplayStatus` の再エクスポート、`SessionAdmin` インターフェース。すべて未使用
- **根拠**: `from "@/lib/types"` でgrep → `_archive/` 配下のモックアップからのみ参照。プロダクションコードから参照0件。各モジュールは直接 `@/lib/generated/prisma` や `@/lib/constants/event` からインポート済み

---

### [D-002] `components/ui/form.tsx` - 未使用のUIコンポーネント
- **優先度**: High
- **該当ファイル**: `components/ui/form.tsx`
- **内容**: shadcn/uiの react-hook-form 連携コンポーネント
- **根拠**: `from "@/components/ui/form"` でgrep → プロダクションコード内0件。独自の `form-field.tsx` が代わりに使われている

---

### [D-003] `components/ui/breadcrumb.tsx` - 未使用のUIコンポーネント
- **優先度**: Medium
- **該当ファイル**: `components/ui/breadcrumb.tsx`
- **内容**: shadcn/uiのパンくずリストコンポーネント
- **根拠**: `from "@/components/ui/breadcrumb"` でgrep → `_archive/` を除くファイルで0件

---

### [D-004] `components/ui/collapsible.tsx` - 未使用のUIコンポーネント
- **優先度**: Medium
- **該当ファイル**: `components/ui/collapsible.tsx`
- **内容**: shadcn/uiの折りたたみコンポーネント
- **根拠**: `from "@/components/ui/collapsible"` でgrep → `_archive/` を除くファイルで0件

---

### [D-005] `components/ui/tabs.tsx` - 未使用のUIコンポーネント
- **優先度**: Medium
- **該当ファイル**: `components/ui/tabs.tsx`
- **内容**: shadcn/uiのタブコンポーネント
- **根拠**: `from "@/components/ui/tabs"` でgrep → `_archive/mockup/` のみ参照

---

### [D-006] `lib/auth/permissions.ts` の `getSession()` - 不要なラッパー
- **優先度**: Medium
- **該当ファイル**: `lib/auth/permissions.ts` (17-19行)
- **内容**: `return auth()` を呼ぶだけの関数
- **根拠**: テストからすら使われていない。プロダクションコードでは `requireAuth()` が直接使用

---

### [D-007] 未使用の型エクスポート群
- **優先度**: Low
- **該当ファイル・型名**:
  - `lib/constants/customer.ts` (55行) - `UserRoleConfigKey`
  - `lib/constants/event.ts` (4行) - `RSVP_STATUSES`
  - `lib/validations/customer.ts` (99-100行) - `CustomerInput`, `CustomerCommunityInput`
  - `lib/validations/login.ts` (13行) - `LoginInput`
  - `lib/validations/event.ts` (44行) - `EventInput`
  - `lib/validations/admin.ts` (40, 70, 87行) - `AdminCreateInput`, `AdminUpdateInput`, `PasswordChangeInput`
  - `lib/helpers/customer-detail.ts` (9行) - `BadgeInfo`
  - `lib/helpers/customer-filter.ts` (14行) - `FilterableCustomerCommunity`
  - `lib/repositories/customer.repository.ts` (40, 61行) - `CustomerCreateData`, `CommunityCreateData`
  - `lib/repositories/event.repository.ts` (8行) - `EventWithCommunityAndRsvps`
- **内容**: エクスポートされているが、プロダクションコードからインポートされていない型・定数
- **根拠**: grep確認済み。一部はテストのみ、一部は完全未使用
- **注意**: `lib/validations/admin.ts`, `lib/validations/event.ts` のスキーマ本体は今後のフェーズで使用予定の可能性あり。型エクスポートのみ不要

---

## その他の改善候補

### [O-001] Badge variant の `as any` - 型定義の根本修正
- **優先度**: High
- **カテゴリ**: 型安全性
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/[id]/_components/customer-detail.tsx` (178, 198, 203, 206, 232, 257, 352, 392行)
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (300行)
  - 他多数、合計12箇所以上
- **現状**: `USER_ROLE_CONFIG`, `RSVP_STATUS_CONFIG` 等の `variant` プロパティが `string` 型だが、Badgeコンポーネントはリテラル型を要求するため `as any` が必要になっている
- **改善案**: 定数の `variant` フィールドの型を Badge の variant リテラル型と一致させる（`as const` の活用等）。1箇所修正すれば12箇所以上の `as any` を一括解消

---

### [O-002] `invite-form.tsx` の `any` 型汚染
- **優先度**: High
- **カテゴリ**: 型安全性
- **該当ファイル**: `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (39, 43, 78, 226, 230行)
- **現状**: `[key: string]: any` のインデックスシグネチャ、`event: any` のevent型未定義、`(r: any)` のrsvps型未定義、`(cc as any).community` の型アサーション連鎖
- **改善案**: `InviteFormProps` の `event` に適切な型を定義。`SerializedCustomer` のインデックスシグネチャを除去し、必要なプロパティを明示

---

### [O-003] `CustomerListFilters` のenum型不一致
- **優先度**: High
- **カテゴリ**: 型安全性
- **該当ファイル**: `lib/repositories/customer.repository.ts` (139, 148行)
- **現状**: `memberCategories` と `auditMemberTypes` が `string[]` として定義されているが、PrismaのEnum型と不一致のため `as any` でキャスト
- **改善案**: `CustomerListFilters` のフィールド型を Prisma の `MemberCategory[]` / `AuditMemberType[]` に変更

---

### [O-004] `CustomerForm` コンポーネントの巨大化 (817行)
- **優先度**: High
- **カテゴリ**: 複雑性
- **該当ファイル**: `app/admin/(authenticated)/customers/_components/customer-form.tsx`
- **現状**: 30以上の `useState` がフラットに並び、`handleSubmit` が約110行。コミュニティデータ構築が3回繰り返されている
- **改善案**: `useReducer` やカスタムフック（`useCustomerForm`）への状態集約。コミュニティ別セクションをサブコンポーネントに分割

---

### [O-005] `InviteForm` コンポーネントの巨大化 (556行)
- **優先度**: High
- **カテゴリ**: 複雑性
- **該当ファイル**: `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx`
- **現状**: 3ステップ（select/customize/confirm）が全て1コンポーネント内
- **改善案**: ステップごとにサブコンポーネントに分割（`InviteSelectStep`, `InviteCustomizeStep`, `InviteConfirmStep`）

---

### [O-006] `exportCustomersAction` の責務過多
- **優先度**: Medium
- **カテゴリ**: 設計
- **該当ファイル**: `lib/actions/customer.actions.ts` (257-357行, 101行)
- **現状**: 認証・データ取得・フィルタリング・CSV生成（ヘッダー定義・行マッピング・BOM付与・エスケープ）が1つのServer Actionに集中
- **改善案**: CSV生成を `lib/helpers/csv-export.ts` に切り出す（テスタビリティも向上）

---

### [O-007] コミュニティコードのハードコード散在
- **優先度**: Medium
- **カテゴリ**: 規約
- **該当ファイル**:
  - `lib/helpers/customer-detail.ts` (40-42行)
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (266-272行)
  - `app/admin/(authenticated)/customers/_components/customer-form.tsx` (159-161行)
  - `app/admin/(authenticated)/customers/[id]/_components/customer-detail.tsx` (144-151行)
  - `app/admin/(authenticated)/events/_components/event-list.tsx` (83-91行)
  - `lib/actions/customer.actions.ts` (104行)
- **現状**: `"venture_auditor"`, `"naikan_meetup"`, `"ai_club"` が多数箇所にハードコード
- **改善案**: `lib/constants/community.ts` にコミュニティコード定数を定義

---

### [O-008] `customer.repository.ts` の `findAll` が120行
- **優先度**: Medium
- **カテゴリ**: 複雑性
- **該当ファイル**: `lib/repositories/customer.repository.ts` (79-198行)
- **現状**: キーワード検索・スコープフィルタ・コミュニティフィルタ・会員区分フィルタ等が全てこの関数内
- **改善案**: 各フィルタ条件の構築を `buildKeywordCondition`, `buildScopeCondition` 等のヘルパー関数に分離

---

### [O-009] Dateシリアライズ時の `as any` キャスト
- **優先度**: Medium
- **カテゴリ**: 型安全性
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/page.tsx` (74行)
  - `app/admin/(authenticated)/customers/[id]/page.tsx` (107行)
- **現状**: DateをISOStringに変換した後、クライアントコンポーネントに渡す際に型が合わず `as any` でキャスト
- **改善案**: R-002のシリアライザ共通化 + R-013の型集約で同時解消

---

## サマリー

| カテゴリ | High | Medium | Low | 合計 |
|---------|------|--------|-----|------|
| 共通化 | 4 | 8 | 2 | 14 |
| 不要コード | 2 | 3 | 2 | 7 |
| その他 | 5 | 4 | 0 | 9 |
| **合計** | **11** | **15** | **4** | **30** |

### 推奨する対応順序

1. **[O-001] Badge variant型の修正** -- 定数定義を1箇所直すだけで12箇所以上の `as any` を一括解消。コスト低・効果大
2. **[D-001][D-002] 未使用ファイル削除** -- `lib/types/index.ts`, `components/ui/form.tsx` 等。即座に実行可能で副作用なし
3. **[R-001] 認証・権限チェックの共通化** -- 全ページに影響し、新規ページ追加時のボイラープレートを大幅に削減
4. **[R-003][R-011] バッジロジック統一 + MEMBER_CATEGORY_LABELS統一** -- 3箇所の微妙に異なる実装は保守上のリスク。セットで対応
5. **[R-002][R-013][O-009] シリアライズ共通化 + 型集約** -- 相互に関連するため同時対応が効率的
6. **[O-002][O-003] invite-form.tsx の型修正 + Enum型統一** -- 型安全性の大幅な向上
7. **[R-004] マスタデータ取得の共通化** -- 実装が単純で効果が大きい
8. **[O-004][O-005] 巨大コンポーネントの分割** -- 影響範囲が大きいため、上記の共通化完了後に着手を推奨
9. **[R-005]-[R-008] 一覧系UIの共通化** -- ページネーション、検索バー、フィルタ等のまとめて対応
