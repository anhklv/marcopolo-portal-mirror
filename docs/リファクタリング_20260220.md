# リファクタリング候補レポート

対象: `app/`, `lib/`, `components/`
スキャン日: 2026-03-09
検出件数: 19件（前回30件 → 解消11件）

---

## 前回（2026-02-20）からの解消状況

| ID | 内容 | 状態 |
|-----|------|------|
| R-001 | 認証・権限チェックの共通化 | **解消** - `getAuthenticatedAdmin()` を作成・全ページで使用中 |
| R-002 | Dateシリアライズ処理の共通化 | **解消** - `lib/serializers/` に集約済み |
| R-004 | マスタデータ取得の共通化 | **解消** - `fetchCustomerFormMasterData()` を作成・使用中 |
| R-005 | ページネーションロジックの共通化 | **解消** - `usePagination` フック + `PaginationControls` コンポーネント |
| R-008 | 配列トグルハンドラの共通化 | **解消** - `useArrayToggle` フック作成・3箇所で使用中 |
| R-011 | MEMBER_CATEGORY_LABELS の散在定義 | **解消** - `lib/constants/customer.ts` からインポートに統一 |
| R-013 | 型定義の散在 | **解消** - `lib/types/serialized.ts` に集約済み |
| D-001 | `lib/types/index.ts` 未使用 | **解消** - ファイル削除済み（`lib/types/serialized.ts` に移行） |
| O-001 | Badge variant の `as any` | **解消** - プロダクションコード内の `as any` は0件 |
| O-004 | `CustomerForm` の巨大化 (817行) | **改善** - `useCustomerForm` フック + サブコンポーネント分割で430行に縮小 |
| O-009 | Dateシリアライズ時の `as any` | **解消** - シリアライザ共通化で解消 |
| R-010 | `checkEventStatus` と `getEventDisplayStatus` の重複 | **解消** - `checkEventStatus` を削除、テストを `getEventDisplayStatus` に移行 |
| D-002 | `components/ui/form.tsx` 未使用 | **解消** - ファイル削除済み |

---

## 共通化の機会

### [R-003] バッジレンダリングの `renderBadges()` 重複（残存）
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (160-170行)
  - `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (239-249行)
- **現状**: `getCustomerBadges()` を呼んで `Badge` に渡すだけの `renderBadges()` が2箇所で同一実装。ロジック自体は `lib/helpers/customer-detail.ts` に集約済みだが、JSX生成部分が重複
- **改善案**: `<CustomerBadges customer={customer} />` 共通コンポーネントを作成
- **影響範囲**: 顧客一覧、イベント招待。推定削減 15-20行

---

### [R-006] コミュニティバッジvariant判定の共通化（残存）
- **優先度**: Medium
- **該当ファイル**:
  - `lib/helpers/customer-detail.ts` (49-51行) - コミュニティcode で分岐
  - `app/admin/(authenticated)/events/_components/event-list.tsx` (52-63行) - 日本語コミュニティ名で分岐
- **現状**: `customer-detail.ts` は `community.code`（`"venture_auditor"` 等）で判定しているが、`event-list.tsx` の `getEventTypeVariant()` は `"ベンチャー監査役の会"` 等の日本語表示名で判定しており一貫性がない
- **改善案**: `lib/constants/community.ts` に `getCommunityBadgeVariant(code: string)` を定義。`event-list.tsx` は `event.community.code` を使うように変更
- **影響範囲**: バッジ表示箇所。推定削減 10-15行

---

### [R-007] フィルタ表示テキスト `getFilterDisplayText()` の重複（残存）
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-list.tsx` (111-124行)
  - `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (218-236行)
- **現状**: コミュニティフィルタの選択状態を表示するテキスト生成関数がほぼ同一実装で2箇所に存在
- **改善案**: `lib/helpers/filter-display.ts` に抽出するか、フィルタPopoverコンポーネントに内包
- **影響範囲**: フィルタUI。推定削減 15-20行

---

### [R-009] 日付フォーマット関数の集約（残存）
- **優先度**: Low
- **該当ファイル**:
  - `lib/utils.ts` (8-14行) - `formatDate()` → `YYYY/MM/DD`
  - `lib/utils/event.ts` (6-23行) - `formatEventDate()` → `YYYY年M月D日(曜) HH:MM`
  - `lib/actions/customer.actions.ts` (375-380行) - `formatDateForCsv()` → `YYYY/MM/DD`
- **現状**: `formatDate()` と `formatDateForCsv()` は同一出力形式（`YYYY/MM/DD`）の別実装。`formatDateForCsv` は `customer.actions.ts` 内のプライベート関数
- **改善案**: `formatDateForCsv` を削除し `formatDate` を使う、または `lib/utils/date.ts` に集約
- **影響範囲**: 日付表示全般。推定削減 10行

---

### [R-012] Server Action内の `getAdminForPermission` 重複（残存）
- **優先度**: Medium
- **該当ファイル**:
  - `lib/actions/customer.actions.ts` (31-54行) - ローカルの `getAdminForPermission()`
  - `lib/auth/permissions.ts` (28-60行) - 公開の `getAuthenticatedAdmin()`
- **現状**: `customer.actions.ts` のローカル関数 `getAdminForPermission()` は `getAuthenticatedAdmin()` と本質的に同じ処理（認証→admin取得→権限情報構築）。4箇所から呼ばれている
- **改善案**: `getAuthenticatedAdmin()` に統一し、`getAdminForPermission()` を削除
- **影響範囲**: `customer.actions.ts` 内の4箇所。推定削減 25行

---

### [R-015] `NONE_VALUE` 定数の重複定義 (**新規**)
- **優先度**: Medium
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/customer-form.tsx` (36行)
  - `app/admin/(authenticated)/customers/_components/audit-community-fields.tsx` (15行)
  - `app/admin/(authenticated)/customers/_components/simple-community-fields.tsx` (13行)
- **現状**: `const NONE_VALUE = "__none__"` が3ファイルで同一定義。Select未選択時のsentinel値として使用
- **改善案**: `lib/constants/form.ts` に定義して3箇所からインポート
- **影響範囲**: 顧客フォーム関連。推定削減 5行

---

### [R-016] `NEXT_REDIRECT` エラーハンドリングの重複 (**新規**)
- **優先度**: Low
- **該当ファイル**:
  - `app/admin/(authenticated)/customers/_components/use-customer-form.ts` (289-296行)
  - `app/admin/(authenticated)/customers/[id]/_components/customer-detail.tsx` (79-84行)
- **現状**: `err instanceof Error && err.message.includes("NEXT_REDIRECT")` の判定が2箇所。今後Server Actionが増えると同じパターンが増加する
- **改善案**: `lib/utils/error.ts` に `isRedirectError(err: unknown): boolean` を定義
- **影響範囲**: 小。推定削減 5行

---

### [R-017] `ActionResult` 型の重複定義 (**新規**)
- **優先度**: Medium
- **該当ファイル**:
  - `lib/actions/auth.ts` (7-9行) - 判別共用体型 `{ success: true; error: null } | { success: false; error: string }`
  - `lib/actions/customer.actions.ts` (22-25行) - オブジェクト型 `{ error?: string; fieldErrors?: Record<string, string[]> }`
- **現状**: 2つの `ActionResult` が異なる構造で定義されている。名前が同一のため混乱を招く
- **改善案**: `lib/types/action.ts` に統一型を定義。用途に応じて `LoginActionResult`, `FormActionResult` 等に命名を区別
- **影響範囲**: Server Action全般。推定削減 5行

---

## 不要コードの削除

### [D-003] `components/ui/breadcrumb.tsx` - 未使用のUIコンポーネント（残存）
- **優先度**: Medium
- **該当ファイル**: `components/ui/breadcrumb.tsx` (109行)
- **根拠**: `from "@/components/ui/breadcrumb"` でgrep → プロダクションコード内0件

---

### [D-004] `components/ui/collapsible.tsx` - 未使用のUIコンポーネント（残存）
- **優先度**: Medium
- **該当ファイル**: `components/ui/collapsible.tsx` (33行)
- **根拠**: `from "@/components/ui/collapsible"` でgrep → プロダクションコード内0件

---

### [D-005] `components/ui/tabs.tsx` - 未使用のUIコンポーネント（残存）
- **優先度**: Medium
- **該当ファイル**: `components/ui/tabs.tsx` (91行)
- **根拠**: `from "@/components/ui/tabs"` でgrep → `_archive/mockup/` のみ参照

---

### [D-006] `lib/auth/permissions.ts` の `getSession()` - 不要なラッパー（残存）
- **優先度**: Medium
- **該当ファイル**: `lib/auth/permissions.ts` (62-64行)
- **内容**: `return auth()` を呼ぶだけの関数
- **根拠**: プロダクションコード・テストのいずれからも未使用

---

## その他の改善候補

### [O-002] `invite-form.tsx` の型定義改善（残存・改善中）
- **優先度**: Medium
- **カテゴリ**: 型安全性
- **該当ファイル**: `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx` (44-55行)
- **現状**: `InviteEvent` インターフェースが最小限の型定義。`rsvps` が `{ customerId: number }[]` と簡略化されている。ただし前回あった `as any` は解消済み
- **改善案**: Prismaの型またはシリアライズ型から生成

---

### [O-003] `CustomerListFilters` の名前衝突（残存）
- **優先度**: Medium
- **カテゴリ**: 型安全性
- **該当ファイル**:
  - `lib/repositories/customer.repository.ts` (30-38行) - サーバーサイド用（Prisma Enum型、オプショナル）
  - `lib/helpers/customer-filter.ts` (4-12行) - クライアントサイド用（string型、必須）
- **現状**: 同名の `CustomerListFilters` が2つ存在し、構造も型も異なる。インポート時に混乱する
- **改善案**: リポジトリ側を `RepositoryCustomerFilters`、ヘルパー側を `UICustomerFilters` 等に改名

---

### [O-005] `InviteForm` コンポーネントの巨大化 (474行)（残存）
- **優先度**: Medium
- **カテゴリ**: 複雑性
- **該当ファイル**: `app/admin/(authenticated)/events/[id]/invite/_components/invite-form.tsx`
- **現状**: 3ステップ（select/customize/confirm）が全て1コンポーネント内。前回556行→474行に縮小したが依然として大きい
- **改善案**: ステップごとにサブコンポーネントに分割

---

### [O-006] `exportCustomersAction` の責務過多（残存）
- **優先度**: Medium
- **カテゴリ**: 設計
- **該当ファイル**: `lib/actions/customer.actions.ts` (259-355行, 97行)
- **現状**: 認証・データ取得・フィルタリング・CSV生成が1つのServer Actionに集中
- **改善案**: CSV生成を `lib/helpers/csv-export.ts` に切り出し

---

### [O-007] コミュニティコードのハードコード散在（残存）
- **優先度**: Medium
- **カテゴリ**: 規約
- **該当ファイル**:
  - `lib/helpers/customer-detail.ts` (49-51行)
  - `lib/actions/customer.actions.ts` (114, 325行)
  - `lib/serializers/customer.ts` (13行)
  - `app/admin/(authenticated)/events/_components/event-list.tsx` (54-58行) - 日本語名でハードコード
- **現状**: `"venture_auditor"`, `"naikan_meetup"`, `"ai_club"` が4ファイルに散在
- **改善案**: `lib/constants/community.ts` にコミュニティコード定数を定義

---

### [O-010] `event-list.tsx` の不要な `getStatusLabel` ラッパー (**新規**)
- **優先度**: Low
- **カテゴリ**: 不要コード
- **該当ファイル**: `app/admin/(authenticated)/events/_components/event-list.tsx` (66-68行)
- **現状**: `function getStatusLabel(status) { return EVENT_STATUS_CONFIG[status].label; }` — 単なるプロパティアクセスのラッパー
- **改善案**: インラインで `EVENT_STATUS_CONFIG[status].label` を直接使用し、関数を削除
- **影響範囲**: 小。推定削減 3行

---

### [O-011] `form-field.tsx` の `any` 型 (**新規**)
- **優先度**: Low
- **カテゴリ**: 型安全性
- **該当ファイル**: `components/ui/form-field.tsx` (8行)
- **現状**: `React.ReactElement<any>` で `eslint-disable` コメント付き
- **改善案**: `React.ReactElement<{ id?: string; 'aria-describedby'?: string; 'aria-invalid'?: boolean }>` に具体化
- **影響範囲**: UIコンポーネント1箇所

---

## サマリー

| カテゴリ | High | Medium | Low | 合計 |
|---------|------|--------|-----|------|
| 共通化 | 0 | 5 | 2 | 7 |
| 不要コード | 0 | 3 | 0 | 3 |
| その他 | 0 | 5 | 4 | 9 |
| **合計** | **0** | **13** | **6** | **19** |

### 前回比

| 指標 | 前回(02/20) | 今回(03/09) | 変化 |
|------|------------|------------|------|
| 総検出件数 | 30 | 19 | -11 (37%削減) |
| High | 11 | 0 | -11 (全解消) |
| `as any` 箇所 | 12+ | 0 | 全解消 |
| 大規模コンポーネント最大行数 | 817行 | 474行 | -343行 |

### 推奨する対応順序

1. **[D-003]-[D-005] 未使用コンポーネント削除** — `breadcrumb.tsx`, `collapsible.tsx`, `tabs.tsx` の3ファイル。副作用なし、合計233行削減
2. **[D-006] `getSession()` 削除** — 未使用ラッパー関数の除去
3. **[R-012] `getAdminForPermission` → `getAuthenticatedAdmin` 統一** — Server Actionと Page Component の認証処理を完全に統一
4. **[R-006][O-007] コミュニティvariant判定 + コード定数化** — セットで対応すると効果的
5. **[R-003][R-007] バッジ・フィルタ表示の共通コンポーネント化** — UIの重複解消
6. **[R-015][R-017] 定数・型の小規模統一** — `NONE_VALUE`, `ActionResult`
7. **[O-005] InviteFormの分割** — 影響範囲が大きいため上記完了後に着手推奨
