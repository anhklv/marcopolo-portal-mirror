stages:
  - verify

default:
  image: node:22
  tags:
    - docker-local

verify:
  stage: verify
  script:
    - node --version
    - npm --version
    - echo "GitLab Runner on Docker Desktop is working"